{
  "name": "dockerhub-mirror-web",
  "version": "1.0.0",
  "description": "Docker Hub 镜像同步服务 - Web 界面",
  "private": true,
  "scripts": {
    "sync": "node sync-data.js",
    "dev": "node sync-data.js && npx serve . -p 7886",
    "build": "echo 'No build step needed - static HTML/CSS/JS'",
    "preview": "node sync-data.js && npx serve . -p 7886"
  },
  "keywords": [
    "docker",
    "mirror",
    "ghcr",
    "container-registry"
  ],
  "author": "freemankevin",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
